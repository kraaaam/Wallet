from event.config import create_consumer

# def run():
#     consumer = create_consumer()

#     for message in consumer:
#         print(message.value)
#         # Do something with the message
#         # For example, insert it into the database
#         insert_into_db(message.value)
